class Solution {
public:
    bool isAcronym(vector<string>& words, string s) {
        string ans;
        for(int i=0;i<words.size();i++){
            string s1=words[i];
            ans+=s1[0];
        }
        if(ans==s)
        return true;
        return false;
    }
};
